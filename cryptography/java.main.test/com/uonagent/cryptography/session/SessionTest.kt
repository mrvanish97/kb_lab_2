package com.uonagent.cryptography.session

internal class SessionTest {
    /*@Test
    fun весело() {
        val session1 = Session(RSA, IDEACFB)
        val session2 = Session(RSA, IDEACFB)
        val publicKey = session1.initializeSession()
        val sessionKey = session2.createSessionKey(publicKey)
        session1.acceptSessionKey(sessionKey)
        val expected = "uygfasdhfkjlserihgvjcweiorlkghfncklrsdhgnfiuckrj" +
                "shjdnovujhkrtshdfviulkjchrntspidfkvhncriutsjkdfhbvniudlkjfgs,dhbnvoueljkrd,fsghdn bivlkj d,rtdf"
        val m = session1.encrypt(expected)
        val actual = session2.decrypt(m) as String
        println(m.contentToString())
        assertEquals(expected, actual)
    }*/
}